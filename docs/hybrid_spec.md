# Hybrid spec：Panic 時改用 LightGBM-v2 排名

一次回答：Panic 時把排名從 Mom20 換成 LightGBM-v2，整體是否比一直用 Mom20 好？

- Production 完全不動，Hybrid 只在分支 `claude/hybrid` 做
- 每條規則在看到結果前寫死於本文件
- 歷史評估只跑一次；FAIL 就停止

凍結日期：2026-09-25。本文件 commit 之後，下列任何定義都不再修改；唯一的修訂（第 10 節）發生在看到任何結果之前。

## 1. 結構

```text
D−1 → Panic gate ─ FALSE → Mom20 排名
                  └ TRUE  → LightGBM-v2 排名
                         ↓
    共用組合層、Active Share、planner、D-Plan
```

- 組合層與 production 相同
  - 前 25 檔等權，投入 95%
  - 續抱區 35 名，換手門檻 10%
  - 最後 3 天不交易
- 平常日 Hybrid 就是 Mom20

## 2. Panic gate

每個決策日 D，只用 D−1 以前的 0050 還原日報酬 `r`：

```text
R20(D−1)  = Π(1 + r) 近 20 日 − 1
vol20(t)  = std(log(1 + r)) 近 20 日（樣本標準差，20 日皆須有值）
threshold = median(vol20) 於資料起點至 D−1 的所有交易日
PANIC     = R20(D−1) < 0  且  vol20(D−1) > threshold
```

- vol20 歷史少於 250 日時 PANIC = FALSE
- 不掃門檻，不換市場代表

## 3. 期間

| 期間 | 用途 |
|---|---|
| 2014 | 資料起點：訓練、特徵與 gate 歷史的暖身 |
| 2015–2024 | 歷史 walk-forward 評估，只跑一次 |
| 2025–2026/09 | 純測試：評估 PASS 後才跑，只跑被選中的版本一次 |

- 所有版本的市場資料都從 2014-01-01 起（config `data.start`）
- 評估窗口：24 交易日，每月月初、月中起點
  - 2015–2021 取 dev 切分，2022–2024 取 validation 切分
  - 跨切分的窗口依既有規則排除
- 150 檔是 2026 名單，早年存活偏誤較大；兩邊同一股票池，配對比較影響較小

## 4. 新資料與可用時間

| 資料 | 來源 | 可用時間 |
|---|---|---|
| 月營收（2013-01 起） | 公開資訊觀測站月報表 `t21sc03`（上市 sii、上櫃 otc） | 月份 m 的營收在次月 11 日（含）收盤後才可用 |
| 上市三大法人（2014-01 起） | TWSE T86 每日報表 | 當日收盤後可用 |
| 上櫃三大法人（2014-12 起，最早可得） | TPEx 三大法人每日報表 | 當日收盤後可用 |

- 法定公布期限是次月 10 日，保守多一天
- 特徵列日期 t 代表「t 收盤後已知」，用於 D = t 的下一個交易日
- 抓不到或無法確認 PIT 的資料一律留空值，不以其他資料替代

## 5. LightGBM-v2 特徵

符號：`P` 為還原價格指數（`action_neutral_return` 累乘），`lr` 為日對數報酬，`m` 為 0050。全部逐股票、只用 t 以前的資料。

| 名稱 | 定義 |
|---|---|
| `r1` `r3` `r5` `r10` `r20` `r60` | `P(t) / P(t−k) − 1` |
| `r20_rank` | 當日 `r20` 的橫斷面百分位排名 |
| `beta60` | 近 60 日 `cov(lr, lr_m) / var(lr_m)` |
| `resid_mom60` | 近 60 日 `Σ(lr − beta60 × lr_m)` |
| `idio_vol60` | 近 60 日 `std(lr − beta60 × lr_m)` |
| `down_vol20` | 近 20 日 `sqrt(mean(min(lr, 0)²))` |
| `dd60` | `P(t) / max(P, 近 60 日) − 1` |
| `dist_high252` | `P(t) / max(P, 近 252 日) − 1` |
| `abn_volume` | 近 5 日平均成交量 ÷ 近 60 日平均成交量 |
| `liquidity` | `log(近 20 日平均 收盤 × 成交量)` |
| `breadth` | 當日 `r20 > 0` 的股票比例（全市場同值） |
| `dispersion` | 當日 `r20` 的橫斷面標準差（全市場同值） |
| `mkt_vol20` | 0050 的 vol20（全市場同值） |
| `rev_yoy` | 最新可用月營收 ÷ 報表上的去年同月 − 1 |
| `rev_mom` | 最新可用月營收 ÷ 報表上的上月 − 1 |
| `rev_yoy_acc` | `rev_yoy` − 前一個月的 `rev_yoy` |
| `rev_yoy_rank` | 當日 `rev_yoy` 的橫斷面百分位排名 |
| `rev_record` | 最新月營收 ≥ 前 12 個月最高者為 1，否則 0 |
| `rev_days` | t 距最新可用月營收公布日（次月 11 日）的日曆天數 |
| `foreign_5d` `foreign_20d` | 外資淨買超股數近 5／20 日合計 ÷ 近 20 日平均成交量 |
| `trust_5d` `trust_20d` | 投信淨買超股數近 5／20 日合計 ÷ 近 20 日平均成交量 |

- 可預測的股票：當日可交易且 `r20` 有值（與 Mom20 相同條件）
- 其他特徵允許空值，由 LightGBM 處理

## 6. 兩個版本

| 版本 | 特徵 |
|---|---|
| v2-A | 第 5 節全部 |
| v2-B | 第 5 節全部 ＋ `open` `high` `low` `close` `volume`（t 日原始數值） |

兩版訓練完全相同，沿用 `lgbm_strategy` 現有程式：

- 標籤：未來 10 日原始報酬（`raw_return`，標籤結束日 ≤ 訓練截止）
- 資料：2014-01-01 起所有已確定的標籤
- 切分：依時間前 80% 訓練、最近 20% 驗證，中間隔 10 日
- 模型：`model.PARAMS`，學習率 0.005，Pearson 提早停止
- C 方式：選好樹數後用 100% 資料重訓（`refit_on_all=True`）
- 重訓：全域日曆上每 5 個交易日一次；決策日 D 用「重訓日 ≤ D」中最新的模型，該模型只用重訓日前一日以前的資料

不再重測：相對強弱標籤、學習率、動能回看天數。

## 7. 通過條件

兩版各自和 Mom20 在 2015–2024 同一組窗口逐窗口配對，`Δ = R(Hybrid) − R(Mom20)`：

| 條件 | 門檻 |
|---|---|
| 配對平均 Δ | > +0.5% |
| 配對中位數 Δ | ≥ 0 |
| 取消資格 | 不多於 Mom20 |
| 成本 | Hybrid 成本 − Mom20 成本 < 平均 Δ |

選版規則：

| 結果 | 動作 |
|---|---|
| 兩版都通過 | 選平均 Δ 較高者 |
| 只有一版通過 | 用那版 |
| 都沒通過 | STOP，維持 Mom20 |

- Panic 日的 LightGBM − Mom20 只做診斷
  - 不調特徵、不調門檻、不挑模型
- FAIL 後不再「拿掉某類特徵再試」「換門檻再試」

## 8. PASS 之後

1. 被選中的版本跑一次 2025–2026/09 純測試，只報告
2. Prospective shadow：到開賽前每天旁跑 Hybrid
   - 存 gate、兩套排名、持股、周轉、之後損益
   - 用來抓實作問題，不宣稱統計 edge
3. Shadow 與 P0 regression 都沒問題，才討論是否升 production

## 9. 執行順序

1. 本文件 commit
2. 背景補資料：月營收 2013 起，法人 2014 起
3. 特徵、gate、walk-forward 預測、Hybrid 策略與因果測試
4. 一次跑完 2015–2024：Mom20、Hybrid v2-A、Hybrid v2-B
5. 依第 7 節判定
6. PASS 才做第 8 節

## 10. 修訂紀錄

| 日期 | 修改 | 原因 | 當時是否看過結果 |
|---|---|---|---|
| 2026-09-25 | • 資料起點 2011 → 2014<br>• 評估 2012–2024 → 2015–2024 | • 縮短期間、減少 2026 名單的存活偏誤<br>• 2015 起上市、上櫃法人資料都齊全<br>• 磁碟空間不足，只保留用得到的年份 | 否：只下載了資料，尚未建特徵、訓練或回測 |

- 2014 是暖身：gate 需要 250 日波動歷史，否則 2015 年 gate 不會啟動
- 原始回應整理成精簡表格後刪除，每筆保留原始回應的 sha256
