# Frozen inputs and release checksums require identical bytes on every checkout.
* -text
*.csv whitespace=cr-at-eol
# Vendored upstream code stays byte-identical to its source commit.
third_party/** -whitespace
